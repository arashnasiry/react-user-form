import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles({
    root: {
        marginTop: 10,
        marginBottom: 10,
    },
    form: {
        margin: 20,
        width: '90%'
    }
})

export { useStyles }